import gate from "../assets/gate.png";

const GateComponent = () => {
  return (
    <img
      src={gate}
      alt="Gate"
      className="flex item-center justify-center size-16"
    />
  );
};

export default GateComponent;
